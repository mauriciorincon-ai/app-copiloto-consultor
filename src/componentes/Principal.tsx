/**
 * La ventana principal (960 × 640) — las pantallas del cuaderno: sesión, permisos, corpus,
 * honestidad, idioma e IA. Las construye la **fase 2** del sprint; aquí solo existe la ventana,
 * para que las tres del plan sean tres de verdad y no dos y una promesa.
 */
export function Principal() {
  return <main className="principal-vacia" aria-busy="false" />;
}
